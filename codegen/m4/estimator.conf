# -----------------------------------------------------------------------------
# Estimator Configuration
# Template -> output mappings
# -----------------------------------------------------------------------------
CLASSNAME="Estimator"

FILES=(
    "$CODEGEN_DIR/estimators/${CLASSNAME}.cpp.m4|$ROOT_DIR/src/estimators/${CLASSNAME}_${NAME}.cpp"
    "$CODEGEN_DIR/estimators/${CLASSNAME}.h.m4|$ROOT_DIR/include/beast/estimators/${CLASSNAME}_${NAME}.h"
    "$CODEGEN_DIR/estimators/${CLASSNAME}_debug.h.m4|$ROOT_DIR/include/beast/estimators/${CLASSNAME}_${NAME}_debug.h"
)
